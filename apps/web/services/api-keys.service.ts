export class ApiKeysService {
  // No backend endpoints or controller currently exist for API keys.
  // Methods will be added when the API exposes API key CRUD operations.
}

export const apiKeysService = new ApiKeysService();
